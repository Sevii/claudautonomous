# Kanboard Configuration
KANBOARD_PORT=8080

# PostgreSQL Configuration
POSTGRES_USER=kanboard
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=kanboard

# Enable plugin installation from UI
PLUGIN_INSTALLER=true
